server:
  port: 8000
  servlet:
    session:
      cookie:
        name: OAUTH2SESSION

spring:
  application:
    name: uaa-server

encrypt:
  key-store:
    location: classpath:buaa.jks
    secret: buaa!@#$
    alias: buaa